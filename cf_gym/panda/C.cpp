#include<iostream>
#include<ctime>
#include<set>
#include<random>
using namespace std;
typedef pair<int,int> pii;
typedef pair<pii,int> ppi;
typedef pair<int,pii> pip;
int main(){
    cin>>n>>m;
    set<ppi> changdu;//按照长度排 第一维是有效长度，第二维是起始位置，第三维是真是长度
    set<pip> shunxv;//按照顺序排，
    piii now;
    set.insert()
    rep(i,1,m){
        cin>>typ;
        if(typ==1){
            ppi now = *changdu.sbegin();
            changdu.erase(now);
            int 
        }
    }
}